import Foundation

/// ios/Vault/Secrets.swift로 복사한 뒤 값을 채우세요.
/// (Supabase 대시보드 → Project Settings → API)
enum Secrets {
    static let supabaseURL = URL(string: "https://YOUR_PROJECT_REF.supabase.co")
    static let supabaseKey: String? = "YOUR_PUBLISHABLE_KEY"
}
